# Backend environment variables

# Node environment ('development' | 'production')
# NODE_ENV=development

# Server
PORT=3001

# CORS — comma-separated list of allowed origins.
# When not set, defaults to allowing all localhost origins (dev mode).
# CORS_ORIGINS=https://app.efb.com,http://localhost:3000

# PostgreSQL
DB_HOST=localhost
DB_PORT=5433
DB_USER=efb
DB_PASS=efb
DB_NAME=efb

# Auth
JWT_SECRET=change-me-in-production
GOOGLE_CLIENT_ID=your-google-client-id
APPLE_BUNDLE_ID=com.efb.app

# Google Cloud Storage (document uploads)
GCS_BUCKET=mobile-efb-dev
GCS_ATIS_BUCKET=efb-atis-dev
# GCS_KEY_FILE=/path/to/gcs-key.json

# Flight filing (Leidos / AFSS)
FILING_USE_MOCK=true
# LEIDOS_VENDOR_USER=
# LEIDOS_VENDOR_PASS=
# LEIDOS_BASE_URL=https://lmfsweb.afss.com/Website/rest

# LiveATC ATIS Transcription (optional — fallback when D-ATIS unavailable)
# OPENAI_API_KEY=sk-your-openai-api-key

# ScrapingBee (used by crawl-liveatc.ts to discover LiveATC feed mount points)
# SCRAPINGBEE_API_KEY=your-scrapingbee-api-key

# Xweather (storm cells, lightning, raster tiles)
XWEATHER_CLIENT_ID=your-xweather-client-id
XWEATHER_CLIENT_SECRET=your-xweather-client-secret
