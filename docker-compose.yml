services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: efb
      POSTGRES_PASSWORD: efb
      POSTGRES_DB: efb
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
