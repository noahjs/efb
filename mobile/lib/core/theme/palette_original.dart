/// Original palette — generic blue accent, narrow surface spread.
///
/// To activate: in app_theme.dart, change the palette import to:
///   import 'palette_original.dart';
library;

import 'package:flutter/material.dart';

// Surfaces
const Color kBackground = Color(0xFF1A1D23);
const Color kSurface = Color(0xFF242830);
const Color kSurfaceLight = Color(0xFF2E323A);
const Color kCard = Color(0xFF2A2E36);
const Color kDivider = Color(0xFF3A3E46);

// Text
const Color kTextPrimary = Color(0xFFFFFFFF);
const Color kTextSecondary = Color(0xFFB0B4BC);
const Color kTextMuted = Color(0xFF6E7280);

// Accent
const Color kPrimary = Color(0xFF4A90D9);
const Color kAccent = Color(0xFF5BA8F5);

// Tab bar
const Color kTabBarBackground = Color(0xFF1E2128);
const Color kTabBarActive = Color(0xFFFFFFFF);
const Color kTabBarInactive = Color(0xFF6E7280);

// Toolbar
const Color kToolbarBackground = Color(0xFF2A2E36);
