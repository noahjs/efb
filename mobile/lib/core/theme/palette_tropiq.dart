/// Tropiq palette — teal accent, wider surface luminance spread.
///
/// To activate: in app_theme.dart, change the palette import to:
///   import 'palette_tropiq.dart';
library;

import 'package:flutter/material.dart';

// Surfaces — wider luminance range (~8% → ~22%)
const Color kBackground = Color(0xFF13161B);
const Color kSurface = Color(0xFF1E2229);
const Color kSurfaceLight = Color(0xFF343840);
const Color kCard = Color(0xFF252930);
const Color kDivider = Color(0xFF3A3E46);

// Text
const Color kTextPrimary = Color(0xFFFFFFFF);
const Color kTextSecondary = Color(0xFFB0B4BC);
const Color kTextMuted = Color(0xFF6E7280);

// Accent — tropical teal
const Color kPrimary = Color(0xFF00BFA5);
const Color kAccent = Color(0xFF26D9B8);

// Tab bar
const Color kTabBarBackground = Color(0xFF161920);
const Color kTabBarActive = Color(0xFFFFFFFF);
const Color kTabBarInactive = Color(0xFF6E7280);

// Toolbar
const Color kToolbarBackground = Color(0xFF252930);
